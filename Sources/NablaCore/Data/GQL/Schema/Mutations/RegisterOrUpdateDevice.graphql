mutation registerOrUpdateDevice(
    $deviceId: UUID,
    $input: DeviceInput!
) {
    registerOrUpdateDevice(deviceId: $deviceId, device: $input) {
        deviceId
        sentry {
            env
            dsn
        }
    }
}
