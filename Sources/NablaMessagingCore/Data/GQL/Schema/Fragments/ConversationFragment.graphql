fragment ConversationFragment on Conversation {
    id
    title
    subtitle
    lastMessagePreview
    unreadMessageCount
    inboxPreviewTitle
    updatedAt
    pictureUrl {
        ...EphemeralUrlFragment
    }
    providers {
        ...ProviderInConversationFragment
    }
}
