fragment ConversationFragment on Conversation {
    id
    title
    subtitle
    lastMessagePreview
    unreadMessageCount
    inboxPreviewTitle
    updatedAt
    providers {
        ...ProviderInConversationFragment
    }
}
