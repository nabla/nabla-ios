# Run this command to update Gemfile.lock whenever you change this file:
#
#   $ bundle check || bundle install --path vendor/bundle --clean
#

source 'https://rubygems.org'

gem 'cocoapods', '~> 1.12.0'
