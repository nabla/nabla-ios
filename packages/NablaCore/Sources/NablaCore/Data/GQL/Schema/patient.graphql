# ----------------------------------------------------------------------------------------------------------------------
# Queries

query TestQuery($id: UUID!) {
    conversation(id: $id) {
        conversation {
            id
        }
    }
}
